// Gamification: streaks, milestones, achievements, scoring

export async function checkAndAwardAchievements(
  _userId: string
): Promise<void> {
  // TODO: check for new milestones, streaks, etc.
}

export async function getDailyScorecard(_userId: string) {
  // TODO: calculate daily stats
  return null;
}
