// On-device OCR using Google ML Kit via expo
// Processes screenshots of gig app earnings screens

export async function processEarningsScreenshot(
  _imageUri: string
): Promise<{ platform: string; amount: number } | null> {
  // TODO: integrate with Google ML Kit on-device OCR
  return null;
}
